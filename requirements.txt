# FastAPI y servidor (ACTUALIZADO)
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Validación y serialización (CRÍTICO: ACTUALIZADO A PYDANTIC V2)
pydantic==2.5.0
pydantic[email]==2.5.0

# Autenticación y seguridad
PyJWT==2.8.0
python-jose[cryptography]==3.3.0
bcrypt==4.1.2
passlib[bcrypt]==1.7.4

# Base de datos MongoDB
motor==3.3.2  # Driver asíncrono de MongoDB para Python
pymongo==4.6.0  # Driver síncrono de MongoDB (dependency de motor)

# Dependencias esenciales para FastAPI
python-multipart==0.0.6
email-validator==2.1.0.post1

# Utilidades
python-dateutil==2.8.2

# Desarrollo y testing (opcionales en producción)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2
